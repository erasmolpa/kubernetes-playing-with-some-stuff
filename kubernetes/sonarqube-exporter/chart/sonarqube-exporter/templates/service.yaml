apiVersion: v1
kind: Service
metadata:
  name: {{ include "sonarqube-exporter.fullname" . }}
  labels:
    {{- include "sonarqube-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sonarqube-exporter.selectorLabels" . | nindent 4 }}
